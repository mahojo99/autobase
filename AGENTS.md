# Relay development

Read `docs/PRD.md`, `docs/BUILD-PROMPT.md`, and `docs/REFERENCES.md` before implementing. This is a new independent portfolio project. Do not import private company memory or modify unrelated repositories.

The primary product is a conversation with a persistent orchestrator that can actually create bots, assign tasks, retrieve shared context, and collect results. Do not reduce it to a provider chat window or static dashboard.

Build in complete vertical slices. Durable state, real engine adapters, and an intelligible desktop experience matter more than feature count. Use current primary documentation and verify installed tool versions. Never invent SDK methods or mark fixture tests as live provider verification.

Keep privileged work outside the Electron renderer. The runtime owns authorization, state transitions, identity, and task execution. Agent output is untrusted input, not authority to grant permissions. Internal reversible configuration requested by the owner should not require repeated confirmation.

Implement and verify the authorized local app autonomously, including ordinary project dependencies, meaningful tests, a Windows package, and documentation. Do not purchase services, publish releases, deploy, send external messages, or alter unrelated user data. Do not copy existing login tokens into the app. Existing supported native authentication may be used without exposing credentials. New paid API access and administrative virtualization prerequisites must remain explicit dependencies if unavailable; continue independent work.

Use the requested build model externally: gpt-6-astra with xhigh reasoning. This does not hardcode the user's future in-app model choices.

Preserve the brief's product intent; make routine engineering decisions and document material deviations. Keep comments and README status factual. Finish with commands run, test results, live versus simulated coverage, package path, and specific remaining blockers. Local commits are allowed; remote publication of implementation is not part of this build handoff.
