# Astra build handoff — Relay

Owner update: the product is now **Autobase**, with automatically assigned Autobot identities and corresponding monochrome portraits, independent of role. The latest Claude subscription request is implemented through unmodified native Claude Code, separately from the API-authenticated SDK. Read the updated PRD note and provider references before applying the original authentication wording below. Current verified scope and blockers are in `docs/verification.md`.

You are implementing Relay in this repository using the user-requested gpt-6-astra with xhigh reasoning. Read AGENTS.md, docs/PRD.md, and docs/REFERENCES.md in full. Then build the product, not just a plan or a static prototype. The owner has authorized this local implementation and validation.

Relay is an original portfolio desktop application: the user talks to one persistent orchestrator, which can set up bots, assign tasks, retrieve shared context, supervise bounded delegation, and return useful results. This orchestrator-first interaction is the central requirement. “Total context” means scoped access to durable workspace knowledge with retrieval and provenance, not putting all history into every prompt.

Build in vertical slices. First make a real Codex conversation produce a persisted task and result in a functioning Electron app. Then implement bot creation through runtime tools, linked child tasks, parent synthesis, shared memory, permissions, cancellation and restart recovery. Implement a second real engine adapter using the official Claude Agent SDK, with supported authentication and honest readiness states. Add local schedules, artifacts, a distinct offline demo workspace, and deliberate desktop polish.

The PRD contains acceptance criteria and the delivery order. The optional local virtual computer is a later milestone. Probe its prerequisites early, but do not allow unavailable virtualization or Claude credentials to block core implementation, tests, packaging, or documentation. Do not imply that fixtures are live API results or that a browser screenshot is an operational virtual computer.

Use Electron, React, TypeScript, SQLite, typed IPC, and a local execution runtime unless verified constraints justify a documented alternative. Keep privileged execution out of the renderer. Consult current primary documentation, inspect installed versions, pin the integration boundary, and test actual protocols. Codex App Server may be experimental: make that limitation explicit. Do not invent SDK functions or promise every model supports every tool. Claude's official SDK authentication restrictions mean you must not implement third-party subscription login without provider approval.

Create a distinctive, calm interface that makes the conversation primary and actual work inspectable. Avoid generic dashboard KPI cards. The user should see real bots, their configurations, delegated tasks, evidence, pending decisions, and useful artifacts. Use tasteful subtle motion, strong typography, good keyboard behavior, and reduced-motion support. Empty, unavailable, and failure states deserve the same care as the happy path.

Make routine implementation decisions independently and record consequential tradeoffs. Install ordinary project dependencies as needed. Use a compatible project-scoped Node runtime if necessary; do not replace global tooling unnecessarily. Do not purchase services, provision paid computers, send external messages, publish releases, or alter unrelated projects. Do not expose or copy provider credentials. If a required external dependency cannot be accessed, report it precisely and continue all independent work.

Run meaningful domain, adapter, integration, and Electron tests. Verify a Windows package actually launches and persists SQLite data. Inspect real app screenshots and fix visual issues. A simulated demo can make offline review easy, but label and isolate it. Live provider tests must use available supported authentication and existing authorization; missing paid API credentials are a verification limitation, not permission to fabricate success.

Deliver working local code, a package where the host permits, an updated README, docs/architecture.md, docs/verification.md, docs/demo-script.md, and screenshots. Record exact commands, pass/fail results, live-versus-fixture coverage, material deviations, and any remaining host/credential blockers. Local commits are allowed; leave implementation publication for the owner. Complete the authorized work rather than stopping after scaffolding, a plan, or the first passing test.
